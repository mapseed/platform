// https://emotion.sh/docs/media-queries#reusable-media-queries-with-object-styles
// We have two media queries for the app: one for devices below 960 pixels in
// width, and one for devices above 960 pixels in width.

export default [`@media (max-width: 960px)`, `@media (min-width: 960px)`];
