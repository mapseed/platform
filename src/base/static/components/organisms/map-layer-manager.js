import React from "react";
import PropTypes from "prop-types";
import { connect } from "react-redux"

const MapLayerManager = props => {
  return <div>HEY!</div>;
};

MapLayerManager.propTypes = {};

export default connect(mapStateToProps)(MapLayerManager);
